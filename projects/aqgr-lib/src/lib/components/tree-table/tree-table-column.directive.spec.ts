import { TreeTableColumnDirective } from './tree-table-column.directive';

describe('TreeTableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
